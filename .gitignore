# 依赖与产物
node_modules/
dist/
dist-ssr/

# 沙箱内重定向的 npm 缓存（避免写到工作区外）
.npm-cache/

# DSH 运行时目录：内含已安装的 skill，不属于站点源码。
# 一并忽略可让 Tailwind v4 的自动源码扫描不把 skill 文档当成待扫描源码。
.dsh/

# 日志与编辑器
*.log
npm-debug.log*
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.local

# 一次性的验证/诊断脚本。这类脚本常在 git add 之后才被创建，
# 不忽略的话很容易被顺手提交进仓库（真发生过一次）。
.tmp-*
